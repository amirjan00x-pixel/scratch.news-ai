# راهنمای راه‌اندازی سیستم خودکار اخبار

## ✅ کارهایی که انجام شد:

### 1️⃣ پنل ادمین (برای تست دستی)
یک صفحه ادمین در آدرس `/admin` اضافه شد که می‌توانید با کلیک روی دکمه، اخبار جدید را دریافت کنید.

**نحوه استفاده:**
1. به آدرس `http://localhost:8080/admin` بروید
2. روی دکمه "Fetch Latest News" کلیک کنید
3. منتظر بمانید تا اخبار دریافت شوند (ممکن است ۳۰-۶۰ ثانیه طول بکشد)

---

### 2️⃣ GitHub Actions (برای خودکارسازی)
یک فایل `.github/workflows/fetch-news.yml` ساخته شد که هر ۶ ساعت یک‌بار به طور خودکار اخبار را دریافت می‌کند.

**نحوه فعال‌سازی:**

#### مرحله ۱: افزودن Secrets در GitHub
1. به مخزن GitHub خود بروید
2. به `Settings` > `Secrets and variables` > `Actions` بروید
3. دو Secret زیر را اضافه کنید:
   - `SUPABASE_URL`: مقدار `VITE_SUPABASE_URL` از فایل `.env`
   - `SUPABASE_ANON_KEY`: مقدار `VITE_SUPABASE_PUBLISHABLE_KEY` از فایل `.env`

#### مرحله ۲: Push کردن کدها
```bash
git add .
git commit -m "Add automated news fetching"
git push origin main
```

#### مرحله ۳: فعال‌سازی Workflow
1. به تب `Actions` در GitHub بروید
2. Workflow به نام "Fetch AI News" را پیدا کنید
3. می‌توانید آن را به صورت دستی هم اجرا کنید (دکمه "Run workflow")

---

## 🔄 زمان‌بندی خودکار
- **هر ۶ ساعت یک‌بار** اخبار جدید دریافت می‌شود
- می‌توانید در فایل `fetch-news.yml` زمان را تغییر دهید:
  - هر ساعت: `'0 * * * *'`
  - هر ۱۲ ساعت: `'0 */12 * * *'`
  - هر روز: `'0 0 * * *'`

---

## ⚠️ نکات مهم
1. **اولین بار**: حتماً یک بار دستی از پنل ادمین اخبار را دریافت کنید تا مطمئن شوید همه چیز کار می‌کند.
2. **هزینه**: GitHub Actions برای مخازن عمومی رایگان است.
3. **لاگ‌ها**: می‌توانید در تب Actions گیت‌هاب، لاگ‌های اجرا را ببینید.
