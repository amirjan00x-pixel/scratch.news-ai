#
# Copy this file to `.env` and fill in your project-specific values.
# Never commit a `.env` file that contains real credentials.
#

VITE_SUPABASE_URL="https://your-project-id.supabase.co"
VITE_SUPABASE_ANON_KEY="public-anon-or-publishable-key"
VITE_SERVER_URL="http://localhost:3001"

# Server-only values documented here for convenience.
# Keep the real secrets in `server/.env`, never in this file.
ADMIN_API_KEY=""
API_ALLOWED_ORIGINS="http://localhost:5173,http://localhost:3000,https://your-frontend-domain.com"
